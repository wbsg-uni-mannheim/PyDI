{
  "attribute": "keywords",
  "model_id": "gpt-5.4-mini",
  "original_value": "Super-resolution (SR) and landmark localization of tiny faces are highly correlated tasks. On the one hand, landmark localization could obtain higher accuracy with faces of high-resolution (HR). On the other hand, face SR would benefit from prior knowledge of facial attributes such as landmarks. Thus, we propose a joint alignment and SR network to simultaneously detect facial landmarks and super-resolve tiny faces. More specifically, a shared deep encoder is applied to extract features for both tasks by leveraging complementary information. To exploit representative power of the hierarchical encoder, intermediate layers of a shared feature extraction module are fused to form efficient feature representations. The fused features are then fed to task-specific modules to detect landmarks and super-resolve face images in parallel. Extensive experiments demonstrate that the proposed model significantly outperforms the state-of-the-art in both landmark localization and SR of faces. We show a large improvement for landmark localization of tiny faces (i.e., 16 × 16). Furthermore, the proposed framework yields comparable results for landmark localization on low-resolution (LR) faces (i.e., 64 × 64) to existing methods on HR (i.e., 256 × 256). As for SR, the proposed method recovers sharper edges and more details from LR face images than other state-of-the-art methods, which we demonstrate qualitatively and quantitatively.",
  "prompt_version": "v2",
  "result": {
    "paraphrase": "Super-resolution (SR) and landmark localization for tiny faces are closely related tasks. Landmark localization can achieve better accuracy when operating on high-resolution (HR) faces, while face SR can leverage prior facial attribute knowledge such as landmarks. Based on this, we propose a joint alignment and SR network that simultaneously detects facial landmarks and super-resolves tiny faces. Specifically, a shared deep encoder extracts features for both tasks by making use of complementary information. To capitalize on the hierarchical encoder’s representational capacity, intermediate layers from a shared feature extraction module are fused to build efficient feature representations. These fused features are then passed to task-specific modules that detect landmarks and super-resolve face images in parallel. Extensive experiments show that the proposed model substantially outperforms state-of-the-art methods in both face landmark localization and SR. We report a large gain in landmark localization for tiny faces, i.e., 16 × 16. In addition, the proposed framework achieves landmark localization on low-resolution (LR) faces, i.e., 64 × 64, at a level comparable to existing methods on HR, i.e., 256 × 256. For SR, the method reconstructs sharper edges and more details from LR face images than other state-of-the-art approaches, as shown both qualitatively and quantitatively."
  },
  "source": "crossref"
}